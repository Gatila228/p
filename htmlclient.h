#include <iostream>
#include "str"

namespace std {
    /// <summary>
    /// вiдправляэ запрос по вказаннiй ссилцi
    /// </summary>
    /// <param name="url"></param>
    /// <returns>Получену Html сторынку</returns>
    str html_request(string url);
}